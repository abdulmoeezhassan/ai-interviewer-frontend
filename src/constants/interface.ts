export interface EditProfileModalProps {
  setShowModal: React.Dispatch<React.SetStateAction<boolean>>;
}

export interface EditWorkExperienceModalProps {
  setShowEditWorkExperienceModal: React.Dispatch<React.SetStateAction<boolean>>;
}

export interface AddCertificationModalProps {
  setShowAddCertificationModal: React.Dispatch<React.SetStateAction<boolean>>;
}

export interface AddProjectModalProps {
  setShowAddProjectModal: React.Dispatch<React.SetStateAction<boolean>>;
}

export interface MyUILayoutProps {
  emilyCall: any;
  callRef: any;
}